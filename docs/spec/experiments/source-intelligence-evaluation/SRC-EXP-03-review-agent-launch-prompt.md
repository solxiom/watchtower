# Review Agent Launch Prompt — SRC-EXP-03

Read the governing source-intelligence draft, SRC-EXP-02 outcome, SRC-EXP-03
protocol, SRC-EXP-03 review contract, runner report, and raw fixture evidence.

Act only as independent evidence reviewer. Treat the report as a claim index,
recompute route readiness from raw evidence, and do not repair or rerun model
calls. Distinguish host-native structural retrieval from ordinary file/grep,
exact outbound tool evidence from a local tools listing, source-view writes
from harmless fixture-external cache, and proven facts from causal hypotheses.

Return exactly `ACCEPT_ROUTE_READY_EVIDENCE` with scope, `REJECT_ROUTES`, or
`NO_ELIGIBLE_ROUTE` or `RESEARCH_BLOCKED`. Write only the permitted owner-only
`.local` review report. Do not amend specs, install/configure anything, clean
fixtures, or commit.
