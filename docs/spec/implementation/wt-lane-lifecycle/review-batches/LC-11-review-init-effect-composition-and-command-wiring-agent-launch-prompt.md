# Agent Launch Prompt — Review Batch LC-11 — Init effect composition and command wiring

Read the shared lane-lifecycle review contract, LC-11 review brief, exact work
brief, accepted predecessor evidence, and the implementation handoff. Review
the exact isolated checkout independently; do not repair it. Reproduce the
installed real-binary proof, refusal matrix, transactional failure matrix,
rollback/recovery checks, architecture/size gates, and Nirvana audit. Emit one
durable final ACCEPT or REJECT only after the worker turn is settled.
