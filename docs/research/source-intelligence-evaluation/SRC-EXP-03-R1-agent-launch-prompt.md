# Agent Launch Prompt — SRC-EXP-03-R1

Read the governing source-intelligence draft, SRC-EXP-02 outcome, original
SRC-EXP-03 protocol/runner prompt, original runner report, independent review,
and [SRC-EXP-03-R1 correction](SRC-EXP-03-R1-correction.md) completely.

Execute only the corrected rerun in its new fixture. Preserve the original
fixture/reports. Use `sudo -u kavan -i` for every command and keep all evidence
owner-only and owned by `kavan`.

Inventory must precede candidate freeze. OpenCode and Claude are mandatory
material candidates. Actually attempt each eligible route's pre-model proof;
do not report a planned capture as evidence or stop incomplete. Use at most one
accepted bounded canary model request per route and no native fallback,
provider install, global/project mutation, paid purchase, T1-T7 task, cleanup,
spec edit, or commit.

Write only `.local/source-intelligence-experiment/SRC-EXP-03-R1-report.md`.
Return exactly `ROUTE_READY` with scope, `NO_ELIGIBLE_ROUTE`, `REJECT_ROUTES`,
or `RESEARCH_BLOCKED`.
