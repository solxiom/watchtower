# Agent Launch Prompt — SRC-EXP-02

Read completely:

1. `AGENTS.md`;
2. `docs/development/engineering-and-review-standard.md`;
3. `docs/research/source-intelligence-rejected.md`;
4. `docs/research/source-intelligence-evaluation/SRC-EXP-01-outcome.md`;
5. `docs/research/source-intelligence-evaluation/SRC-EXP-02-corrected-provider-evaluation.md`.

Execute only SRC-EXP-02 in the new disposable fixture. This is research, not
Watchtower implementation.

Before mutation, declare every repository/commit/tree/worktree, provider and
host identity, install/cache/home/config/log path, process/transport/port,
network behavior, dependency action, measurement/capture method, allowed
Watchtower report, and cleanup target. Stop if the retained SRC-EXP-01 fixture,
an original/active worktree, a global configuration, or a project dependency
would be modified.

Stop unless Watchtower's tracked tree is clean and committed, the preparer's
public execution bundle exists, and the hidden-evidence digest/byte-count
commitment has been copied into the preflight record. Do not request or inspect
the hidden packet.

Run only `PROVIDER_READINESS` first. Do not send comparative task prompts until
all readiness checks pass and the immutable readiness attestation exists. A
direct list-tools result is not model-visible schema proof. Capture the exact
sanitized outbound tool schema, correlate a real model/provider canary call,
and do not infer an unobserved host timing cause.

If readiness passes, follow the independent preparer's precommitted 14-run
schedule exactly. Use fresh contexts, one run at a time, identical conditions,
the corrected T7 prompt, complete measurement, and no hidden native fallback.
Never treat a failed, incomplete, or unaccepted output as a saving.

Return exactly `ADOPT_BASELINE`, `ADOPT_LIMITED`, `REJECT_PROVIDER`, or
`RESEARCH_BLOCKED`. Write only
`.local/source-intelligence-experiment/SRC-EXP-02-report.md`, owned by `kavan`.
Do not amend specs, add product dependencies, transfer spike code, clean the
fixture, or commit.
