# watchtower Help Assets

This directory contains the help fragments for the watchtower CLI.

- `help.json` — assembled help registry (generated from fragments)
- `commands/` — per-command help fragments (`*.hlp.json`)
- `groups/` — command group definitions (`*.hlp.json`)
