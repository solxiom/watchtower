{
  "name": "upgrade",
  "group": "basic",
  "summary": "Preview or atomically apply a compatible managed runtime upgrade.",
  "description": "Preview or atomically apply a compatible managed runtime upgrade.",
  "usage": "wt upgrade [--lane=<slug-or-uuid>] [--workspace=<path>] [--to=<version>] [--apply] [--allow-downgrade] [--dry-run] [--json] [--no-color]",
  "flags": [
    {"name": "lane", "description": "Select one relevant lane by exact ID or slug.", "optional": true},
    {"name": "workspace", "description": "Resolve the lane from this existing repository or control-home path.", "optional": true},
    {"name": "to", "description": "Select an explicit compatible runtime version to preview or apply.", "optional": true},
    {"name": "apply", "description": "Atomically apply the previewed upgrade: stage and verify every managed asset, then write install.json last.", "optional": true},
    {"name": "allow-downgrade", "description": "Authorize an explicit downgrade to an older runtime version whose manifest declares the lane schema backward-compatible.", "optional": true},
    {"name": "dry-run", "description": "Render the read-only upgrade preview without changing lane state.", "optional": true},
    {"name": "json", "description": "Emit one versioned command-result envelope.", "optional": true},
    {"name": "no-color", "description": "Disable ANSI color in human output.", "optional": true}
  ]
}
