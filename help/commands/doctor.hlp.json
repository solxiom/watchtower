{
  "name": "doctor",
  "group": "basic",
  "summary": "Validate lane-local markers, configuration, bindings, permissions, Git-ignore coverage, the installed runtime, account access, watcher heartbeat, pack index, coordinator queue/cursor, and operator-session index/turn state without repair; --tui also qualifies the promoted terminal target.",
  "usage": "wt doctor [--workspace=<path>] [--lane=<slug-or-uuid>] [--tui] [--json] [--no-color] [--verbose]",
  "flags": [
    {"name": "workspace", "description": "Resolve the lane from this existing repository or control-home path.", "optional": true},
    {"name": "lane", "description": "Select one relevant lane by exact ID or slug.", "optional": true},
    {"name": "tui", "description": "Also qualify the promoted TUI terminal target tuple for this invocation.", "optional": true},
    {"name": "json", "description": "Emit one versioned command-result envelope whose data is doctorReport.", "optional": true},
    {"name": "no-color", "description": "Disable ANSI color in human output.", "optional": true},
    {"name": "verbose", "description": "Include expanded diagnostic details.", "optional": true}
  ]
}
