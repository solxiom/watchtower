{
  "name": "config",
  "group": "basic",
  "summary": "Show resolved lane identity, bindings, paths, and redacted strict configuration.",
  "usage": "wt config show [--workspace=<path>] [--lane=<slug-or-uuid>] [--initiative=<id>] [--json] [--no-color] [--verbose]",
  "flags": [
    {"name": "workspace", "description": "Resolve the lane from this existing repository or control-home path.", "optional": true},
    {"name": "lane", "description": "Select one relevant lane by stable ID or slug.", "optional": true},
    {"name": "initiative", "description": "Filter lane selection to one initiative ID.", "optional": true},
    {"name": "json", "description": "Emit one versioned command-result envelope with the same redaction as human output.", "optional": true},
    {"name": "no-color", "description": "Disable ANSI color in human output.", "optional": true},
    {"name": "verbose", "description": "Include bounded resolution and validated repository/config count diagnostics.", "optional": true}
  ]
}
